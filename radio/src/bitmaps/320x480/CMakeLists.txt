string(TOLOWER ${PCB} PREFIX)

if(PCB STREQUAL X10)
  set(REVERSE "${REVERSE}")
endif()

add_bitmaps_target(${PREFIX}_bitmaps "${RADIO_SRC_DIRECTORY}/bitmaps/320x480/bmp_*.png" 480 5/6/5${REVERSE})
add_bitmaps_target(${PREFIX}_calibration_bitmaps "${RADIO_SRC_DIRECTORY}/bitmaps/320x480/calibration/bmp_*.png" 480 5/6/5${REVERSE})
add_bitmaps_target(${PREFIX}_button_bitmaps "${RADIO_SRC_DIRECTORY}/bitmaps/320x480/button/alpha_*.png" 480 4/4/4/4${REVERSE})
add_bitmaps_target(${PREFIX}_alpha_bitmaps "${RADIO_SRC_DIRECTORY}/bitmaps/320x480/alpha_*.png" 480 4/4/4/4${REVERSE})
add_bitmaps_target(${PREFIX}_alpha_calibration_bitmaps "${RADIO_SRC_DIRECTORY}/bitmaps/320x480/calibration/alpha_*.png" 480 4/4/4/4${REVERSE})
add_bitmaps_target(${PREFIX}_masks ${RADIO_SRC_DIRECTORY}/bitmaps/320x480/mask_*.png 480 8bits)
add_bitmaps_target(${PREFIX}_slider_masks "${RADIO_SRC_DIRECTORY}/bitmaps/320x480/slider/*.png" 480 8bits)
add_bitmaps_target(${PREFIX}_layouts_masks "${RADIO_SRC_DIRECTORY}/gui/320x480/layouts/*.png" 480 8bits)
add_bitmaps_target(${PREFIX}_themes_bitmaps "${RADIO_SRC_DIRECTORY}/gui/320x480/themes/*.png" 480 5/6/5${REVERSE})
add_bitmaps_target(${PREFIX}_fonts ${RADIO_SRC_DIRECTORY}/fonts/320x480/*.png 480 8bits)
add_bitmaps_target(${PREFIX}_volume_masks ${RADIO_SRC_DIRECTORY}/bitmaps/320x480/volume/*.png 480 8bits)
add_bitmaps_target(${PREFIX}_bootloader_bitmaps ${RADIO_SRC_DIRECTORY}/bitmaps/320x480/bootloader/bmp_*.png 480 5/6/5${REVERSE})
add_bitmaps_target(${PREFIX}_bootloader_icons ${RADIO_SRC_DIRECTORY}/bitmaps/320x480/bootloader/icon_*.png 480 8bits)
add_dependencies(${PREFIX}_bitmaps ${PREFIX}_calibration_bitmaps ${PREFIX}_button_bitmaps ${PREFIX}_alpha_bitmaps ${PREFIX}_alpha_calibration_bitmaps ${PREFIX}_masks ${PREFIX}_slider_masks ${PREFIX}_layouts_masks ${PREFIX}_themes_bitmaps ${PREFIX}_fonts ${PREFIX}_volume_masks ${PREFIX}_bootloader_bitmaps ${PREFIX}_bootloader_icons)
